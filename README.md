# Land-Valuation-System
Production-ready backend for land valuation using the Market Method, built with FastAPI, async SQLAlchemy, and SQLite. Implements a full benchmark selection pipeline that analyzes transactions, cleans and normalizes data, removes outliers, and selects representative parcels using configurable rules and scoring.
